sudo apt update
//update your debian

sudo apt install build-essential
//g++ and gcc and makefile

sudo apt install gdb
//debugger

sudo apt install libglu1-mesa-dev freeglut3-dev mesa-common-dev
//openGL (who knows it's important)

sudo apt install libglew-dev
//installing GLEW (Open GL Extension Wrangler)

sudo apt install libsdl2-dev libsdl2-2.0-0
//SDL2 Base install

sudo apt install libjpeg-dev libwebp-dev libtiff5-dev libsdl2-image-dev libsdl2-image-2.0-0
//SDL Image

sudo apt install libmikmod-dev libfishsound1-dev libsmpeg-dev liboggz2-dev libflac-dev libfluidsynth-dev libsdl2-mixer-2.0-0
//SDL Mixer (Audio)

sudo apt install libfreetype6-dev libsdl2-ttf-dev libsdl2-ttf-2.0-0
//SDL True Type Fonts

sudo apt install clang
//Install clang
